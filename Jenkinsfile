pipeline {
	agent { label "agentfarm"}
	stages{


		stage('Delete the workspace') {
			steps{
				cleanWs()
			}
		
		}
                stage('two') {
                        steps{
                             echo "two"
                        }

                }
                stage('three') {
                        steps{
                              echo "three"
                        }

                }

	}






}
